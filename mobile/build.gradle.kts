// Top-level build file for the MOCCA Android client.
// Plugin versions are pinned here so every Gradle invocation uses the same
// Android Gradle Plugin + Kotlin compiler combination.
plugins {
    id("com.android.application") version "8.4.0" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
